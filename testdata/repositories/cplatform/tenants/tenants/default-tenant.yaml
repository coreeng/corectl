name: default-tenant
kind: app
parent: parent
description: "Default tenant to use in tests"
contactEmail: parent@prod.domain
environments:
  - dev
  - prod
adminGroup: admin-group@prod.domain
readonlyGroup: readonly-group@prod.domain
cloudAccess: []
repos: []
